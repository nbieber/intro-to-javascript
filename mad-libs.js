// Create a function startMadLibs

function startMadLibs() {

	// For each span in the document
	for(             ) {
		// Get the class of that span

		// Ask the user for a word of that type

		// Replace the contents of the span
	}
}